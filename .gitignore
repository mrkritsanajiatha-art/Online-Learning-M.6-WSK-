node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
seed_*.js

# ===== ห้ามขึ้น GitHub: repo นี้เปิดสาธารณะ ใครก็เข้าดูได้ =====
# ข้อสอบจริง (ไฟล์ต้นฉบับจากคุณครู)
*.pdf
# เฉลยข้อสอบ — ลงฐานข้อมูลไปแล้ว เก็บไว้เป็นสำเนาในเครื่องเท่านั้น
seed_midterm.json
seed_midterm_content.sql
# ไฟล์ export รายชื่อผู้สมัคร — มีชื่อ เบอร์โทร อีเมล และสถานะการชำระเงิน
registrations_*.csv
registrations_*.xlsx
